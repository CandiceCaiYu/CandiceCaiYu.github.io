'use client';

// `app/dashboard/CustomSection.tsx` is the UI for the `/dashboard` URL

export default function Page() {

    return (
        <div>
            <div>content</div>
        </div>
    )
}
